version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 12
    commands:
      - cd pipeline
      - npm install
      - npm install -g aws-cdk
  build:
    commands:
      - npm run build
      - cdk deploy -y
